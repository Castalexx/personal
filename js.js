/* 
Hacer un loop rebote para el video
 */
id = setInterval(frame, 5);

function frame() {
  if (el video llega a su fin) {
    clearInterval(id);
  } else {
    /* code to change the element style */  empezar desde atras}
}

/* Hacer animaciones para los iconos */

